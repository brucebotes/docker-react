language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t brucebotes/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true brucebotes.docker-react npm run test

